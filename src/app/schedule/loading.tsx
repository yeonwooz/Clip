import styles from './schedule.module.css';

const LoadingSchedule: React.FC = () => {
    return <div className={styles.loadingPageContainer}>로딩중...</div>;
};

export default LoadingSchedule;
